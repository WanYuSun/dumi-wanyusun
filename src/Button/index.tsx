// src/components/Button.tsx

/** @changeset Button */

import React, { type FC } from 'react';

const Button: FC<{ title: string }> = (props) => (
  <h4>{props.title} wanyu.sun</h4>
);

export default Button;
